---
- hosts: windows
  gather_facts: no
  tasks:
  - win_domain_membership:
      dns_domain_name: ***
      domain_admin_user: ***
      domain_admin_password: ***
      domain_ou_path:
      state: domain
    register: domain_state

  - win_reboot:
        state: reboot
